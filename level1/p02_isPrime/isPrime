#include<stdio.h>
#include<math.h>
int main()
{
	int a, b, r;
	float c;
	r=scanf_s("%d",&a);
	c = sqrt(a);
	if (r == 1)
	{
		for (b = 2; b < c+1; ++b)
		{
			if (a % b == 0)break;
		}
		if (b > c) printf("yes");

		else printf("no");
	}
	else printf("error");
}
