//加密与解密
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
	int i;
	char str[5] = {};
	char output[5] = {};
	printf("请输入明文\n");
	scanf_s("%s", &str);
	for (i = 0; i < 5; i++)
	{
		output[i] = str[i] + i + 5;
	}
	str[i] = '0';
	printf("加密后的文字是：%s", output);
}
